{
  "model_name": "efficientnet_b0",
  "num_classes": 6,
  "image_size": 224,
  "checkpoint_path": "models/checkpoints/best_model.pth",
  "onnx_path": "models/onnx/model.onnx",
  "status": "training pending; backend falls back to mock inference until ONNX export exists"
}
